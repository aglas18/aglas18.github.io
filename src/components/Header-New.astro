<section class="masthead">
  <div class="header">
    <div class="subheading">adrian glasmyre</div>
    <h1>Creative and Self-Motivated Designer</h1>
    <p>
      Passionate about transforming ideas into engaging digital experiences.
      Specialized in delivering website redesigns and digital assets that
      balance design, UX, and brand storytelling. Detail-oriented and
      collaborative with a strong understanding of how design translates across
      platforms to create seamless, user-centered experiences.
    </p>
    <img
      class="decorative-flower mobile"
      src="/flower.svg"
      width="100"
      height="100"
      alt="decorative flower"
    />
  </div>
  <div class="header-desktop">
    <img
      class="decorative-flower desktop"
      src="/flower.svg"
      width="100"
      height="100"
      alt="decorative flower"
    />
  </div>
</section>

<style>
  .header {
    position: relative;
    border-radius: 20px;
    padding: 130px 20px 40px;
    margin-top: 20px;
    overflow: hidden;
    display: flex;
    flex-direction: column;
    align-items: flex-start;
  }

  .header::before {
    content: "";
    position: absolute;
    inset: 0;
    background: var(--gradient-accent);
    z-index: 0;
  }

  .header::after {
    content: "";
    position: absolute;
    inset: 0;
    background: url("../assets/mobile-header-background.png") center / cover
      no-repeat;
    opacity: 0.3;
    z-index: 1;
  }

  .header > * {
    position: relative;
    z-index: 2;
  }

  .header h1 {
    margin-top: 0px;
  }

  .header h1,
  .header p {
    color: var(--platinum);
    margin-bottom: 0px;
  }

  .header p {
    font-size: 20px;
    line-height: 29px;
  }

  .header .subheading {
    color: var(--dust-gray);
    margin-bottom: 0px;
  }

  .decorative-flower {
    position: absolute !important;
    top: 25px;
    right: 25px;
  }

  .header-desktop {
    display: none;
  }

  @media (min-width: 1024px) {
    .header::before {
      display: none;
    }

    .header::after {
      display: none;
    }

    .masthead {
      display: flex;
      flex-direction: row;
    }

    .header {
      width: 50%;
    }

    .header .subheading {
      color: var(--amaranth);
    }

    .header h1,
    .header p {
      color: var(--ink-black);
    }

    .header {
      padding-bottom: 0px;
    }

    .header-desktop {
      display: block;
      background: var(--gradient-accent);
      width: 50%;
      border-radius: 20px;
      position: relative;
      overflow: hidden;
    }

    .header-desktop::after {
      content: "";
      position: absolute;
      inset: 0;
      background: url("../assets/desktop-header-background.png") center / cover
        no-repeat;
      opacity: 0.5;
      z-index: 1;
    }

    .decorative-flower.mobile {
      display: none;
    }

    .decorative-flower.desktop {
      bottom: 25px;
      right: 25px;
      width: 200px;
      height: 200px;
      top: auto;
    }

    .subheading {
      font-weight: bold;
      font-size: 28px;
    }
  }
</style>
